{{#each students as |student|}}
  {{!-- {{log student}} --}}
  <li data-id="{{student.id}}">
    <p><strong>Name:</strong>
      {{#if student.name}}
        {{student.name}}
      {{else}}
        No name provided.
      {{/if}}
    </p>
    <p><strong>Grade:</strong>
      {{#if student.grade}}
        {{student.grade}}
      {{else}}
        No grade level provided.
      {{/if}}
    </p>
    <p><strong>Check-in note:</strong>
      {{#if student.checkedIn}}
        {{student.checkedIn}}
      {{else}}
        No note provided.
      {{/if}}
    </p>
      <button class="update-student">Update this student</button>
      <button class="delete-student">Delete this student</button>
    {{!-- <button type="submit" class="btn btn-danger delete-student">Remove</button> --}}
    {{!-- {{> partial}} --}}
  </li>

{{/each}}